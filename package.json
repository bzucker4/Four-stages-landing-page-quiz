{
  "name": "four-stages-landing-page-quiz",
  "private": true,
  "version": "1.0.0",
  "description": "Four Stages of Consciousness quiz landing site",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step: static site, deployed as Cloudflare Workers static assets (see wrangler.jsonc).'",
    "test": "tsx scripts/worker.test.mts",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^5.6.0",
    "wrangler": "^4.131.2"
  }
}
