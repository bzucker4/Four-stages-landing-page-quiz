# Expected images

Drop these files in this folder before launch. Filenames are already wired into the page `<meta>` tags, so keep the names exact.

- `og-four-stages.jpg` — Open Graph / Twitter Card image for `/index.html`. Recommended size 1200x630.
- `og-victim.jpg` — Open Graph / Twitter Card image for `/quiz/victim.html`. Recommended size 1200x630.
- `og-creator.jpg` — Open Graph / Twitter Card image for `/quiz/creator.html`. Recommended size 1200x630.
- `og-witness.jpg` — Open Graph / Twitter Card image for `/quiz/witness.html`. Recommended size 1200x630.
- `og-unity.jpg` — Open Graph / Twitter Card image for `/quiz/unity.html`. Recommended size 1200x630.

This folder is otherwise empty on purpose (static site, no build step, nothing to generate).
